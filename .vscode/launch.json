{
    // 使用VS Code调试Python脚本的launch配置
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: train.py fit",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/train.py",
            "args": [
                "fit",
                "--config",
                "./configs/test.yaml"
            ],
            "console": "integratedTerminal"
        }
    ]
}